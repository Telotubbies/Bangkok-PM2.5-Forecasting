# Bangkok Environmental Data Warehouse - Step 1 Ingestion
# Python 3.10+

pandas>=2.0.0,<3.0.0
numpy>=1.24.0,<2.0.0
pyarrow>=14.0.0
requests>=2.28.0
urllib3>=2.0.0

# Optional: for concurrent IO (max 4 threads)
# Standard library concurrent.futures is used; no extra deps required
